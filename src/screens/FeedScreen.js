import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const FeedScreen = () => {
  return (
    <SafeAreaView>
      <Text>Feed</Text>
    </SafeAreaView>
  );
};

export default FeedScreen;
